apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod-readiness-http
spec:
  containers:
    - name: nginx-pod-readiness-http
      image: nginx
      readinessProbe:
        initialDelaySeconds: 5
        periodSeconds: 5
        httpGet:
          path: /ready
          port: 80